# 《烬誓：圣女与影刃》新会话接续 Prompt

你接手轻舟游戏工作室的 Ashen 项目。GitHub 恢复仓库：

https://github.com/YuxiangLiu-lyx/Ashen

先实际读取仓库，再开发；不要仅依赖会话记忆或自行从旧版重建。按以下步骤执行：

1. 已有工作目录先检查 Git 状态、远端及当前提交，保护未提交文件；没有目录则 clone 上述仓库。fetch GitHub 的 `main`，在保留本地工作的前提下整合最新内容，不强制覆盖。不要替换另一个平台正在使用的远端。
2. 先读 `AGENTS.md`、`source/README.md`、README 指向的 `CURRENT_STATE.json`、`source/GLOBAL_PROMPT.md`，再读当前版本的需求、总结、剧情全文、战斗与奖励规则、美术对应表、Agent 记录及 QA。恢复所需入口、资源路径及命令以仓库文档为准。
3. 先核对最新实际版本与可恢复范围。产品版本、平台版本、运行源码 SHA、GitHub 备份 SHA 是不同字段，以发布回执和真实文件为证。历史版本仅供追溯；遇到缺失文件先在仓库历史及归档中查找，不能假称已恢复。
4. 执行本次用户需求，按需分派独立任务，由主整合者统一整合与验收。遵守现有角色形象、剧情知识边界、存档兼容和资源规则；不要擅自把 `FUTURE_ONLY` 构思实装。
5. 新代码、文案、原图、衍生图、资源映射及修改记录均写入项目可恢复的文件；下载地址只能作为来源记录，不能代替资源文件。旧资产/旧稿保留历史，缺失内容如实记录。保护用户私人存档，不上传凭据。
6. 每轮结束更新当前状态、需求完成度、总结和 QA，执行必要检查，然后检查差异、`git add`、`git commit`、`git push` 至 `YuxiangLiu-lyx/Ashen` 的 `main`。用户已经授权正常推送，无需再次确认。不得强推或删除既有历史。
7. 推送后核验远端分支实际包含本轮提交；交付时提供提交链接和 SHA。只有验证成功才说“已上传”。如推送失败，保留本地提交及可恢复文件，明确说明失败原因和待上传 SHA。保存、测试、网站发布分别报告，不互相冒充。

本次具体开发需求：按当前用户消息执行。
